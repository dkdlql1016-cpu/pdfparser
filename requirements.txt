# Direct runtime dependencies for this project only.
# Do not put .venv freeze output here unless we intentionally want a fully locked environment.

Flask>=3.0.0
PyMuPDF>=1.24.0
opendataloader-pdf>=2.4.7
